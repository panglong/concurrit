Add the following to your bash profile file.

export CONCURRIT_HOME=$HOME/Repository/Research/ParLab/concurrit
export LD_LIBRARY_PATH=$CONCURRIT_HOME/bin/:$LD_LIBRARY_PATH
export DYLD_LIBRARY_PATH=$CONCURRIT_HOME/bin/:$DYLD_LIBRARY_PATH
export LD_LIBRARY_PATH=$CONCURRIT_HOME/glog/lib/:$LD_LIBRARY_PATH
export DYLD_LIBRARY_PATH=$CONCURRIT_HOME/glog/lib/:$DYLD_LIBRARY_PATH
export BOOST_ROOT=$HOME/Repository/Research/ParLab/Boost/boost_1_48_0

To install CONCURRIT (including glog, pintool, etc.)
$CONCURRIT_HOME/scripts/install_concurrit.sh

To compile and install the pintool
$CONCURRIT_HOME/scripts/install_pintool.sh

To compile and install glog
$CONCURRIT_HOME/scripts/install_glog.sh
